const editList = (() => {


    function saveList(oldName, newName) {
        
    }

    function deleteList(listName){

    }

    return{
        saveList, deleteList, 
    }
})();

export {editList};